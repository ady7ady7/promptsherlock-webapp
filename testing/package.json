{
  "name": "image-analyzer-tests",
  "version": "1.0.0",
  "description": "Test suite for image analyzer application",
  "type": "module",
  "scripts": {
    "test": "node scripts/run-all-tests.js",
    "test:quick": "node scripts/run-all-tests.js --quick",
    "test:frontend": "cd ../frontend && npm test",
    "test:backend": "cd ../backend && npm run test:upload && npm run test:env"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "chalk": "^5.3.0",
    "axios": "^1.6.0",
    "cross-spawn": "^7.0.3",
    "ora": "^7.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}